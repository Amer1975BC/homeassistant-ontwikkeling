template:
  - sensor:
      - name: "Gemiddeld Verbruik Ochtend"
        unique_id: gemiddeld_verbruik_ochtend
        unit_of_measurement: "kWh"
        device_class: energy
        state_class: measurement
        icon: mdi:weather-sunny
        state: >
          {% set verbruik = states('sensor.zonneplan_electricity_consumption_today') %}
          {% if verbruik not in ['unknown', 'unavailable', None] %}
            {{ (verbruik | float(0) * 0.25) | round(2) }}
          {% else %}
            0
          {% endif %}

      - name: "Gemiddeld Verbruik Middag"
        unique_id: gemiddeld_verbruik_middag
        unit_of_measurement: "kWh"
        device_class: energy
        state_class: measurement
        icon: mdi:weather-partly-cloudy
        state: >
          {% set verbruik = states('sensor.zonneplan_electricity_consumption_today') %}
          {% if verbruik not in ['unknown', 'unavailable', None] %}
            {{ (verbruik | float(0) * 0.35) | round(2) }}
          {% else %}
            0
          {% endif %}

      - name: "Gemiddeld Verbruik Avond"
        unique_id: gemiddeld_verbruik_avond
        unit_of_measurement: "kWh"
        device_class: energy
        state_class: measurement
        icon: mdi:weather-sunset
        state: >
          {% set verbruik = states('sensor.zonneplan_electricity_consumption_today') %}
          {% if verbruik not in ['unknown', 'unavailable', None] %}
            {{ (verbruik | float(0) * 0.30) | round(2) }}
          {% else %}
            0
          {% endif %}

      - name: "Gemiddeld Verbruik Nacht"
        unique_id: gemiddeld_verbruik_nacht
        unit_of_measurement: "kWh"
        device_class: energy
        state_class: measurement
        icon: mdi:weather-night
        state: >
          {% set verbruik = states('sensor.zonneplan_electricity_consumption_today') %}
          {% if verbruik not in ['unknown', 'unavailable', None] %}
            {{ (verbruik | float(0) * 0.10) | round(2) }}
          {% else %}
            0
          {% endif %}
